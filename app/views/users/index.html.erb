<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user) %>
      <%= link_to "<i class='icon-trash'></i>".html_safe, user_path(user), :method => :delete, data: { confirm: "You sure?" } %>
    </li>
  <% end %>
</ul>
